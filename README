homework: converted index.html to jquery - saved as index_jquery.html
homework: add input value as a new item on the list. can't figure out --
1.why is there is a break after the bullet? - the items in append() are not being added in the order they are listed. <li></li> then the entry field val
ANSWER - USE "+" instead of ","
2.how to append the DOM elements ("<li>",(entry_field.val()),"</li>") to the array taskList, so the function striker applies (toggling line-through)
